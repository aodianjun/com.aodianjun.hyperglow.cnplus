## 安装包用途说明
- **`hyperglow-cnplus-release-v0.3.82-109.apk`（正式版）**：正常使用请安装此版本。正式签名、R8 压缩，适合日常安装使用。
- **`hyperglow-cnplus-debug-v0.3.82-109.apk`（调试版）**：提 issue 反馈问题时请安装此版本，可提供详细诊断日志辅助排查。调试签名，仅用于测试与问题排查，请勿作为日常版本长期安装。

---

## 更新日志 · v0.3.82 (versionCode 109)

### 修复

- **AOD 切歌后整首无歌词（issue #11）**：网易云共享内存的位置按"base 元组 + 墙钟 × 速度"续算，Doze 冻结的是 base 更新，切歌后写入端仍停在旧歌时间线上导致位置持续越界。0.3.81 的越界钳制把越界值钳到歌尾并清空歌词行，造成整首歌 AOD 无歌词并伴随 60Hz `capping` 日志刷屏。现改为：切歌后首个位置须满足可信上界门控，超过歌长 +2s 容差一律视为 stalled，从最后可信基准外推到投影歌尾稳定占位。正常播放 / 暂停 / seek / 单曲循环以及 issue #3/#9/#10 的修复不受影响。
- **新 HyperOS 设备被误报"不支持配置文件"（上游 `6216fdc`）**：移除 SystemUI/AOD 版本号白名单门禁，改为按符号探测实际解析出的能力判断——有可用 surface 即"可用"，并修复诊断报告误报 setup failed 的问题。本地实验模式覆写逻辑保持不变。
- **诊断报告拒绝 "available" 状态（上游 `c5b1ffa`）**：与新状态机配套的校验放行，避免诊断报告被拒收。
- **行级扫光越界**：`fillEndMs` 允许越过本行行尾（跨行填充是合法数据），渲染端钳制行窗口，防止行级扫光越过行尾。

### 新增功能

- **中文歌拒绝错误的日语假名注音（上游 `8422d78`）**：数据源（网易云）常给中文歌词错误标注日语 furigana。现在中文歌检测到 ruby 注音含日语假名（平/片假名）时拒绝显示注音和罗马音；日语歌的假名注音是正确数据，原样保留。

### 优化

- **兼容性状态改为能力计数展示（上游 `6216fdc`）**：UI 显示"n/16 个挂钩可用"计数，取代旧的置信度标签。
- 新增回归测试：切歌残留拒绝与冻结残留、2x 倍速跟踪、越界残留外推、假名注音拒绝、"available" 状态解码等；全部 564 个测试通过。

---

**Full Changelog**: https://github.com/aodianjun/com.aodianjun.hyperglow.cnplus/compare/108-0.3.81...109-0.3.82