> ⚠️ **测试版本请勿下载** — 此版本为预发行测试版，仅供测试与问题排查，请勿用于日常使用。
## 安装包用途说明
- **`hyperglow-cnplus-release-v0.3.93-120.apk`（正式版）**：正常使用请安装此版本。正式签名、R8 压缩，适合日常安装使用。
- **`hyperglow-cnplus-debug-v0.3.93-120.apk`（调试版）**：提 issue 反馈问题时请安装此版本，可提供详细诊断日志辅助排查。调试签名，仅用于测试与问题排查，请勿作为日常版本长期安装。

---

## 更新日志 · v0.3.93 (versionCode 120)【预发行】

### 修复 · 旋转回落仍被打断导致歌词不可见（issue #31）

- **根因**：0.3.92 让 `resolveAodRotationStep` 竖持返回 `PORTRAIT` 后，旋转仍无法回落——加速度计被周期性**重新附着**（画布挂载/快照眼前随 `renderable` 抖动触发 detach→attach），而 `attach` 的首次附着分支会无条件把 `currentStep` 置 null、把 `filteredX/Y` 归零并清空候选窗口。于是 `LANDSCAPE→PORTRAIT` 回落所需的连续稳定样本（约 1 秒）每次都因重新附着被清零，永远无法落地 → 画布卡在横屏逻辑帧、被裁出竖屏可视区，横/竖屏都看不到歌词。
- **修复（保留朝向 + 首样本直取）**：
  - `attach` 首附着分支不再重置判定状态，detach 后的重附着沿用已知朝向；`stop()`（热重载退役）才做真正的冷启动归零。
  - 重附着且已有真实重力读数时立即按当前向量重新推导，朝向不等首个新样本即落位。
  - 滤波器新增首样本直取：冷启动首帧直接用真实重力而非从 `(0,0)` 慢收敛，避免把横屏读数在收敛期误判成竖屏。

---

**Full Changelog**: https://github.com/aodianjun/com.aodianjun.hyperglow.cnplus/compare/119-0.3.92...120-0.3.93