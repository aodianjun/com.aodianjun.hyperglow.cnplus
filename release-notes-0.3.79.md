
## 安装包用途说明
- **`hyperglow-cnplus-release-v0.3.79-106.apk`（正式版）**：正常使用请安装此版本。正式签名、R8 压缩，适合日常安装使用。
- **`hyperglow-cnplus-debug-v0.3.79-106.apk`（调试版）**：提 issue 反馈问题时请安装此版本，可提供详细诊断日志辅助排查。调试签名，仅用于测试与问题排查，请勿作为日常版本长期安装。

---

## 更新日志 (v0.3.79 / versionCode 106)

### 🌟 修复息屏歌词约 45 秒后消失（issue #3，本次核心修复）
- **根因**（0.3.78 实机复现 + 反编译确认）：息屏进入 Doze 后，播放器共享内存写入端被冻结，真实位置停止更新，模块靠墙钟外推补偿。旧逻辑外推超过 `MAX_EXTRAPOLATION_MS`（45s）即标记 `positionUnknown` 并**清空当前歌词行**——于是每次息屏约 45s 后歌词必然消失。
- **修复**：外推超过 45s 预算但仍在歌曲时长内时，**继续推进歌词位置而不是清空**。Doze 冻结（音乐仍在播、写入端可能整首歌不恢复）与写入端死亡是两种不同场景，只有外推越过歌尾（或无时长信息）才判定写入端死亡、切换占位。歌词现可整首歌持续滚动，直到真实位置恢复（亮屏）或到达歌尾。
- 恢复真实位置后立即回到真实值（含回绕/循环兼容），暂停时外推冻结等既有语义不变。
- 新增回归测试：外推超预算但在歌曲时长内继续推进、真实位置恢复后回切等场景。

> 与 v0.3.78 的差异仅此一处行为修复；v0.3.78 的下一行歌词颜色修复等内容全部包含。
