
## 安装包用途说明
- **`hyperglow-cnplus-release-v0.3.78-105.apk`（正式版）**：正常使用请安装此版本。正式签名、R8 压缩，适合日常安装使用。
- **`hyperglow-cnplus-debug-v0.3.78-105.apk`（调试版）**：提 issue 反馈问题时请安装此版本，可提供详细诊断日志辅助排查。调试签名，仅用于测试与问题排查，请勿作为日常版本长期安装。

---

## 更新日志 (v0.3.78 / versionCode 105)

> 相比 v0.3.70（versionCode 89）

### 🌟 修复"下一行歌词颜色"设置无效（本次核心修复）
- **根因**：下一行歌词在实机有两条绘制路径——非行级同步路径（`drawNextLine`）用的是正确的 `nextLineText` 颜色 token；但**行级同步路径**（最常见配置）把下一行当成普通副行（音标/翻译）绘制，用的是 `secondaryText`。颜色设置从未传到画笔上，所以无论怎么改，实机都不变色；而预览一直用 `nextLineText`，两端自然对不上。
- **修复**：两条路径统一使用 `nextLineText` 独立 token；下一行透明度对齐预览（0.45）。新增回归测试锁定 token 分离，防止再次混用。

### ✨ 实机发光效果与预览完全一致（v0.3.77 修复，随本版继续有效）
- 此前所谓"共享渲染管线"只覆盖逐字时间源一条路径，实机最常用的两条路径仍在跑旧的手工发光实现（行级同步路径的软扫光 shader + 逐字叠加；LRC 行级歌词的五层绘制 + 呼吸脉冲——呼吸/脉冲效果预览里根本没有），这就是"实机发光和预览完全两样"的真正原因。
- 三条路径（行级同步 / LRC 行级 / 逐字时间源）全部收敛到统一入口，委托共享渲染核心 `LyricGlowRenderer`：dim 底（sung 色 30% 不透明度）、光晕、easeInOutCubic 缓动扫光带（28% 光带占比、5 段渐变含白色峰值高光与长拖尾）。**配方只定义一次，AOD、锁屏、预览三端效果由构造保证一致。**
- 删除约 630 行旧发光死代码，从根上杜绝再次漂移。

### ✨ 息屏渲染与外观定制（v0.3.77 修复，随本版继续有效）
- **新增"实时时钟跟随"息屏开关**：修复歌词布局与系统时钟重叠。开启时按原始时钟边界放置；关闭时走增强锚定 + 看门狗路径。
- **修复歌曲信息字体大小设置实际不生效**：设置值真正参与 AOD 歌词块高度计算，高字号内容不再被裁切。
- **恢复字体 / 歌曲信息 / 下一行歌词颜色定制**：六种预设（暖金、冰蓝、薄荷绿、樱花粉、柠檬黄、薰衣草紫）+ 自定义 hex；预览与实机渲染同源、编辑即时反映。
- **恢复外观设置实时预览组件**：编辑调色板 / 发光即时预览，效果与实机一致。
- **新增 AOD 作用域 `com.miui.aod`** 及独立进程 hook，适配 MIUI 独立 AOD 进程架构。

### 🧹 稳定性与防护加固（v0.3.77 恢复，随本版继续有效）
- **恢复系统时钟看门狗**：stock 时钟漂移 >24px 强制重排、渲染停滞看门狗、40s 锚定保持、burn-in 容器解析。
- **恢复 Lyricon 生产端静默死亡防护**：position 看门狗 + 静默重订阅、墙钟外推播放、换歌后陈旧位置拒绝、300ms 单调恢复容差带。
- **恢复 SuperLyric 下一行支持**：SDK 的 `secondary` 字段即下一行歌词；行事件陈旧窗口由 3s 放宽到 12s，避免长间奏被误判切回退链。
- **恢复 LyricInfo 会话回退与位置恢复**：无歌词注入模块时回退到任意活跃媒体会话，不丢歌名 / 歌手 / 进度。
- **恢复"暂停后显示内容"管线**：AOD 与锁屏双端暂停滞留锚定。
- **新增 MIUI greeze 冻结链防冻结**：保护正在播放的媒体应用不被冻结导致歌词卡死。
- **恢复投影传输防护**：传输间隙边缘时间戳标注 + 终态隐藏缓存清理。

### 🔧 LyricInfo 歌词源适配（v0.3.77 修复，随本版继续有效）
- **修复精简版（Lite）歌曲信息带上当前歌词行**：检测到原生逐行 payload 复用 `songName`/`artist` 字段的格式时，改用系统 MediaMetadata 干净元数据。
- **修复精简版歌词完全丢失**：原生 payload 中数字字段不再让严格反序列化抛异常丢弃整个 payload，改为宽松提取。
- **修复精简版翻译不显示**：补 `transLyric` 字段回退。

### 🧪 测试与构建
- 恢复 Robolectric 测试依赖；恢复 / 新增大量单测（524+ 用例全过）：发光管线判定与扫光几何、下一行颜色 token 分离回归、Lyricon 静默死亡防护、SuperLyric 下一行、LyricInfo 会话选择 / 宽松 payload 解析、画布布局、锁屏暂停滞留、SceneCompiler 编译白名单等。
- 恢复繁体中文（zh-rTW）字符串，默认 / 繁体 locale 键完全对齐，消除 release lint 告警。
- 恢复应用内更新检查 `VersionCheck`（最新 release 查询 / 本地 APK SHA256 / 版本比对）。
- CI：移除机器特定 aapt2 override、工作流触发与发布步骤升级；发布自动刷新正式版 / 调试版说明。

## 待修复清单
- AOD 逐字（卡拉 OK）效果待完善：发光开启时统一为整块扫光，与预览一致。
- 发光路径帧耗尚待实机采样，验证 doze 预算内不掉帧。
- 本次问题排查在设备上短暂开启了"诊断日志"取证，请在体验后到模块内将该开关手动关闭，恢复最佳性能。

---
> 本版本为当前最新正式版（Latest）。下载过测试 / 预发行版本的设备，检测到 versionCode 105 高于预发行版即可收到更新提示。

---
