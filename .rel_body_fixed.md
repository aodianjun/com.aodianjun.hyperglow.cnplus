## 安装包用途说明
- **`hyperglow-cnplus-release-v0.3.65-77.apk`（正式版）**：正常使用请安装此版本。正式签名、R8 压缩，适合日常安装使用。
- **`hyperglow-cnplus-debug-v0.3.65-77.apk`（调试版）**：提 issue 反馈问题时请安装此版本，可提供详细诊断日志辅助排查。调试签名，仅用于测试与问题排查，请勿作为日常版本长期安装。

---

# HyperGlow v0.3.65

> 对比上一个正式版 **v0.3.1**（2026-08-05）的更新内容。

## 修复（息屏 / 锁屏歌词）
- **息屏歌词不更新**：锁屏待机时歌词停止推进 → 改用墙钟时间做位置外推，息屏期间歌词持续滚动。
- **单曲循环越界**：外推位置越过歌曲末尾时按歌曲时长取模回绕，不再需要亮屏重置。
- **切歌残留位置**：歌曲切换后拒绝上一首歌的残留位置，避免显示错乱。
- **拖动进度条后歌词不更新**：seek 后拒绝回退的旧位置（3 秒残留窗口），歌词跳到应有位置。
- **锁屏歌词闪烁 / 卡片色值丢失**：surface 联动时保留锁屏卡片颜色与透明度。
- **AOD 歌词被歌名压制**：歌词激活时优先显示歌词，不再被歌曲元数据覆盖；AOD 常显歌曲元数据绕过歌词状态门禁。

## 体验优化
- 歌词字号预设选择器（多档文本大小）。
- 歌词块高度（maxHeightFraction）外观设置。
- 概览页新增权限检查状态：通知权限 / 前台服务 / 电池优化。
- 概览页支持歌词源切换与实时状态展示。
- 实验模式开关透传到 hook 端。

## 技术 / 构建
- 前台服务类型 `dataSync` → `specialUse`。
- 增强对 HyperOS 组件重命名的容错（burn-in 容器查找）。
- CI 双变体 debug 构建：`hyperglow-cnplus-debug-*.apk`（诊断日志开关可控，默认仅 w/e 级错误日志）与编译期硬关 trace 的变体。
- 新增 **release 变体**：R8 压缩 + 正式签名，体积大幅缩小（约 4.7 MB），作为发布用版本。
- GitHub Actions 自动将 debug / release APK 发布为 Release 资产。

## 本次包含的构建
- **`hyperglow-cnplus-release-v0.3.65-77.apk`**：正式签名、R8 压缩的发布版（体积最小，推荐安装）。
- **`hyperglow-cnplus-debug-v0.3.65-77.apk`**：诊断日志开关可控（默认仅 w/e 级错误日志）。

> `hyperglow-cnplus-debug-*.apk` 为调试签名 debug 构建，仅供测试与问题排查，请勿用于生产。
> `hyperglow-cnplus-release-*.apk` 使用正式签名密钥构建，可直接安装。