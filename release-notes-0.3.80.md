
## 安装包用途说明
- **`hyperglow-cnplus-release-v0.3.80-107.apk`（正式版）**：正常使用请安装此版本。正式签名、R8 压缩，适合日常安装使用。
- **`hyperglow-cnplus-debug-v0.3.80-107.apk`（调试版）**：提 issue 反馈问题时请安装此版本，可提供详细诊断日志辅助排查。调试签名，仅用于测试与问题排查，请勿作为日常版本长期安装。

---

## 更新日志 (v0.3.80 / versionCode 107)

### 🌟 修复 AOD 歌词播完后永久卡占位 + 60Hz 日志刷屏（issue #9）
- **根因**（0.3.79 实机日志确认，两处叠加）：
  1. **外推越过歌尾后每帧死循环**：`recomputeAndEmit` 歌尾分支清空歌词行后把 `extrapolating` 置回 false，但**不钳制外推基准**——下一个 stalled 回调重新进入外推、又立刻越过歌尾、又清空行，60Hz 反复"越界→清空→占位"，日志刷屏 + 状态重建风暴。
  2. **越界真实位置直接放行**：Doze 下共享内存交付的真实位置基准本身已远超歌曲时长（日志 `base=424922ms` vs `duration=172913ms`），每个越界值都被原样接受，进一步触发歌尾分支抖动。
- **修复**：
  - `advanceExtrapolation()` 新增**歌尾稳定钳制**：外推一旦越过歌曲时长，位置直接钳到 `duration` 并保持，不再翻转外推标志；
  - `recomputeAndEmit()` 歌尾分支统一处理（清行 + 稳定占位 + 日志只在状态实际变化时输出一次），同时覆盖真实位置越界的路径；
  - `positionUnknown`（写入端死亡判定）仅保留给无时长信息的源（如 LRC 行级）。
- 语义变化：外推越过歌尾后**稳定钳在歌尾占位**（旧行为是冻结在 45s 预算值），等真实位置恢复（亮屏/回绕/切歌/seek）后立即恢复选行。
- 新增回归测试：外推越过歌尾一次性钳制并保持稳定、越界真实位置（含两值交替到达）钳制不抖动、恢复后歌词回归。

> v0.3.78/0.3.79 的下一行歌词颜色修复、息屏 45s 歌词消失修复（issue #3）等内容全部包含。
