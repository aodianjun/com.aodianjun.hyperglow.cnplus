## 安装包用途说明
- **`hyperglow-cnplus-release-v0.3.76-103.apk`（正式版）**：正常使用请安装此版本。正式签名、R8 压缩，适合日常安装使用。
- **`hyperglow-cnplus-debug-v0.3.76-103.apk`（调试版）**：提 issue 反馈问题时请安装此版本，可提供详细诊断日志辅助排查。调试签名，仅用于测试与问题排查，请勿作为日常版本长期安装。

---

## 更新日志 (v0.3.76 / versionCode 103)

> 相比 v0.3.70（versionCode 89）

### ✨ 息屏渲染与外观定制
- **新增"实时时钟跟随"息屏开关**：修复歌词布局与系统时钟重叠的问题。开启时按原始时钟边界放置、不做防抖；关闭时走增强锚定 + 看门狗路径，两者可自由切换。
- **修复歌曲信息字体大小设置实际不生效**：此前设置值未参与 AOD 歌词块高度计算；现让其真正生效，并将歌词块高度下界固定到最小值，避免高字号内容被裁切。
- **恢复字体 / 歌曲信息 / 下一行歌词颜色定制**：支持暖金、冰蓝、薄荷绿、樱花粉、柠檬黄、薰衣草紫六种预设（亦可直接填 hex）。UI 与渲染端共用编译白名单判定（`#RGB` / `#RRGGBB` / `#AARRGGBB`），预览与实机渲染同源、编辑即时反映。
- **恢复外观设置实时预览组件**：编辑调色板 / 发光即时预览，效果与实机一致。
- **新增 AOD 作用域 `com.miui.aod`** 及其独立进程 hook，适配 MIUI 独立 AOD 进程架构，保证歌词渲染在不同进程架构下均生效。

### 🌟 实机发光效果与预览完全同步（本次核心修复）
- **根因**：所谓"共享渲染管线"实为两份各自演化的实现——预览是手写的 Compose 绘制，AOD / 锁屏实机走另一份 `LyricGlowRenderer`，两者早已漂移：光带占比实机 40% vs 预览 28%、实机线性扫光 vs 预览 easeInOut 缓动、实机 3 段渐变（无高光）vs 预览 5 段（白色峰值高光 + 长拖尾）、dim 底取色不同。
- **修复**：`LyricGlowRenderer` 重写为预览配方（band 28%、easeInOutCubic 缓动、5 段渐变 `[已唱常亮→白峰高光→拖尾→渐隐]`、dim = sung 色 30% 不透明度），且**预览改为直接委托该渲染器**——配方只定义一次，AOD、锁屏、预览三端效果由构造保证一致，彻底杜绝双份代码手工同步造成的再次漂移。

### 🧹 稳定性与防护加固（恢复快照合并回退的关键逻辑）
- **恢复共享发光渲染管线 `LyricGlowRenderer`**：统一预览与实机三层绘制（dim 底色、光晕、扫光带），修复发光效果回退、以及开启发光后已唱 / 未唱无法区分的问题。
- **恢复系统时钟看门狗**：stock 时钟漂移 >24px 强制重排、渲染停滞看门狗、40s 锚定保持、burn-in 容器解析，杜绝系统时钟漂移导致的歌词错位。
- **恢复 Lyricon 生产端静默死亡防护**：position 看门狗 + 静默重订阅（SDK 回调可静默死亡，实测 stale age 519s 连接仍显示 CONNECTED）、墙钟外推播放、换歌后陈旧位置拒绝（避免闪错词）、300ms 单调恢复容差带。
- **恢复 SuperLyric 下一行支持**：SDK 的 `secondary` 字段即下一行歌词，此前被硬编码为空导致下一行渲染路径从不绘制；同时将行事件陈旧窗口由统一 3s 放宽到 12s，避免长间奏被误判为源死亡而在歌曲中途切换回退链。
- **恢复 LyricInfo 会话回退与位置恢复**：无歌词注入模块 / Lyricon 死亡时回退到任意活跃媒体会话，不丢歌名 / 歌手 / 进度；恢复换歌时外推状态重置，以及频道切回时共享内存延迟导致的 1..300ms 单调容差带。
- **恢复"暂停后显示内容"（pauseShowContent）管线**：AOD 与锁屏双端暂停滞留锚定（LyricRetentionAnchor），暂停时是否保留歌曲信息 / 歌词可独立开关。
- **新增 MIUI greeze 冻结链防冻结**：hook MIUI freeze 冻结链路，保护正在播放的媒体应用不被冻结，避免后台播放被冻结导致歌词卡死。
- **恢复投影传输防护**：传输间隙边缘时间戳标注 + 终态隐藏缓存清理。
- **恢复 AodStateProjector `PLAYING_PLACEHOLDER` 占位**与 XiaomiCapabilityStore 能力回退。

### 🔧 LyricInfo 歌词源适配（对齐模块 README）
- **修复精简版（Lite）歌曲信息带上当前歌词行**：原生逐行 payload 把"当前歌词行"填入 `songName`、把"歌名 - 歌手"填入 `artist`；现检测到该格式改用系统 MediaMetadata 干净元数据，同时消除逐行误触发换歌导致的外推状态重置。
- **修复精简版歌词完全丢失**：原生 payload 中 `songId` 等字段可能为数字，严格反序列化抛异常导致整个 payload 被丢弃；改为宽松提取，绝不丢整个 payload。
- **修复精简版翻译不显示**：补 `transLyric` 字段回退（QQ 音乐原生输出的翻译放在 `transLyric`）。

### 🧪 测试与构建
- 恢复 Robolectric 测试依赖；恢复 / 新增大量单测：Lyricon 923 行、SuperLyric 下一行与 12s 陈旧窗口、LyricInfo 会话选择 / 位置恢复 / 宽松 payload 解析、发光管线判定与扫光几何（新增缓动曲线 / 光带占比 / dim alpha 锁定测试）、画布布局、锁屏暂停滞留、SceneCompiler 编译白名单等。
- 恢复繁体中文（zh-rTW）字符串，清理 14 个孤儿键使默认 / 繁体 locale 键完全对齐（均为 364 键），并同步翻译模板。
- 恢复应用内更新检查 `VersionCheck` 与 `CallerValidator` 缓存测试钩子。
- CI：移除机器特定 aapt2 override、工作流触发与发布步骤升级。

## 待修复清单
- AOD 逐字（卡拉 OK）效果待完善：发光开启时统一为整块扫光，与预览一致。
- 发光路径帧耗尚待实机采样，验证 doze 预算内不掉帧。
- 本次问题排查在设备上短暂开启了"诊断日志"取证，请在体验后到模块内将该开关手动关闭，恢复最佳性能。

---
> 本版本为当前最新正式版（Latest）。下载过测试 / 预发行版本的设备，检测到 versionCode 103 高于预发行版即可收到更新提示。

---